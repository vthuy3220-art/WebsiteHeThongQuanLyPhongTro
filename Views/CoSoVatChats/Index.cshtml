@model IEnumerable<HeThongQuanLyPhongTro.Models.CoSoVatChat>

@{
    ViewData["Title"] = "Quản lý cơ sở";
}

<div class="space-y-6">
    <div class="flex justify-between items-center">
       
        <div class="flex space-x-3">
            <a asp-action="Create" class="bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg transition-all flex items-center">
                <i class="fas fa-plus mr-2"></i> Thêm cơ sở mới
            </a>
        </div>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 rounded">
            <i class="fas fa-check-circle mr-2"></i> @TempData["Success"]
        </div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 rounded">
            <i class="fas fa-exclamation-circle mr-2"></i> @TempData["Error"]
        </div>
    }

    <div class="bg-white rounded-xl shadow-md p-4">
        <form asp-action="Index" method="get" class="flex gap-4">
            <div class="flex-1">
                <input type="text" name="searchString" value="@ViewBag.SearchString"
                       placeholder="Tìm kiếm theo tên cơ sở..."
                       class="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>
            <div>
                <button type="submit" class="bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600 transition-all">
                    <i class="fas fa-search mr-2"></i> Tìm kiếm
                </button>
            </div>
        </form>
    </div>

    <div class="bg-white rounded-xl shadow-md overflow-hidden">
        <div class="overflow-x-auto">
            <table class="w-full">
                <thead class="bg-gray-50">
                    <tr>
                        <!-- Đã sửa tiêu đề cột cho khớp với Cơ sở vật chất -->
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Mã vật chất</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Tên vật chất</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Tình trạng</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Mô tả</th>
                        <th class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Thao tác</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    @foreach (var item in Model)
                    {
                        <tr class="hover:bg-gray-50 transition-all">
                            <!-- Đã thay đổi các biến item cho khớp với Model CoSoVatChat -->
                            <td class="px-6 py-4">@item.MaCSVC</td>
                            <td class="px-6 py-4 font-medium">@item.TenThietBi</td>
                            <td class="px-6 py-4">@item.TinhTrang</td>
                            <td class="px-6 py-4">@item.SoLuong</td>
                            <td class="px-6 py-4">
                                <div class="flex justify-center space-x-3">
                                    <a asp-action="Details" asp-route-id="@item.MaCSVC"
                                       class="text-blue-500 hover:text-blue-700" title="Chi tiết">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <a asp-action="Edit" asp-route-id="@item.MaCSVC"
                                       class="text-yellow-500 hover:text-yellow-700" title="Sửa">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <a asp-action="Delete" asp-route-id="@item.MaCSVC"
                                       class="text-red-500 hover:text-red-700" title="Xóa">
                                        <i class="fas fa-trash"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @if (!Model.Any())
        {
            <div class="text-center py-12">
                <i class="fas fa-building text-gray-300 text-5xl mb-3"></i>
                <p class="text-gray-500">Chưa có dữ liệu cơ sở</p>
                <a asp-action="Create" class="inline-block mt-3 text-blue-500 hover:underline">Thêm cơ sở đầu tiên</a>
            </div>
        }
    </div>
</div>